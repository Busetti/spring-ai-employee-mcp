server:
  port: 8083

spring:
  application:
    name: employee-chat-client

  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: llama3.2:1b

    mcp:
      client:
        type: SYNC
        sse:
          connections:
            employee-mcp:
              url: http://localhost:8082
